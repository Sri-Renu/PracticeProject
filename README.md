📘 JSON Data Processor

A simple Python CLI project to manage student records stored in a JSON file.
This project demonstrates CRUD operations (Create, Read, Update, Delete) with persistent storage.

---

## 🚀 Features
- 📂 **Load & Save Data** – Stores all student data in `students.json`
- 👀 **View Students** – Display all student records in a clean format
- 🔍 **Search by ID** – Find a student by their unique ID
- ➕ **Add Student** – Insert new student records interactively
- ✏️ **Update Student** – Modify an existing student’s details
- ❌ **Delete Student** – Remove a student by ID
- 🖥️ **Interactive Menu** – Easy-to-use console interface

---
